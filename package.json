{
  "name": "access-ai",
  "private": true,
  "description": "Root scripts to run the full local stack (Postgres + sign-inference + Node API + Vite)",
  "scripts": {
    "dev": "bash scripts/dev-all.sh",
    "dev:docker": "bash scripts/dev-docker-stack.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "packageManager": "pnpm@9.15.4"
}
